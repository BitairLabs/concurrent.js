{
  "scripts": {
    "lint": "npx tsc --noEmit",
    "build": "bash ./scripts/build.sh",
    "start": "npm run build && npx http-server static"
  },
  "dependencies": {
    "http-server": "^14.1.1",
    "@bitair/concurrent.js": "^0.9.0",
    "extra-bigint": "^1.2.1"
  },
  "devDependencies": {
    "@tsconfig/strictest": "^2.0.8",
    "esbuild": "^0.28.0",
    "typescript": "^6.0.3"
  },
  "license": "MIT"
}
